// ============================================================
// config.ts — Technical configuration only.
// No user-facing copy lives here.
// ============================================================

export const config = {
  newsletter: {
    // Redirect URL after form submission — not user-editable copy
    confirmationUrl: "https://parmarproperties.in/newsletter-confirmed",
  },
};
